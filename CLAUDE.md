<div dir="rtl" style="text-align: right;">

# قسم تسليمات المكتب — Package Transfer To Office (`Package-Transfer-To-Office`)

![version](https://img.shields.io/badge/worker-v1.2.0-blue)

**بتعمل إيه:** بتسجّل انتقال الطرد من **المخزن للمكتب**. الموظف بيسكن باركود
الأوردر، والأداة بتكتب `custom.package_whereabouts_s1` (أو `_s2` لدورة
الاستبدال/الاسترجاع) بقيمة **`Office`**.
**مين بيستخدمها:** المخزن
**الإصدار:** `1.2.0` (`WORKER_VERSION` في `index.js`)

> 🔴 **الريبو ده Worker وبس — مفيش واجهة هنا ومفيش نسخة مستقلة** (قرار أحمد
> 15-09-2026). الواجهة الوحيدة هي **`office-transfer.html`** جوّه
> `Warehouse-Operations-Center`. ⛔ ممنوع يتضاف `index.html` هنا: نسخة مستقلة
> معناها مفتاح `localStorage` تاني وسر تاني وشاشة دخول تانية — وكلهم بنود
> مفتوحة في الأدوات اللي اتحوّلت للهب، ومفيش سبب نفتحهم من جديد.

> 🔴 **والأداة دي «الأداة رقم ٣» في خطة `package_whereabouts`** المسجّلة في
> `ecommoda-order-lifecycle` → `references/package-whereabouts.md` §5. المهارة
> بتوصفها على إنها **أداة واحدة بقايمتين**؛ قرار أحمد 15-09-2026 إنها
> **أداتين**: دي (القايمة الأولى — مخزن → مكتب)، والتانية (شبكة الأمان —
> الطرد المنسي في المكتب يرجع للمخزن) في تمريرة منفصلة. **المهارة هتتحدّث
> من أحمد.**

## الروابط

```
الـ Worker  : https://package-transfer-to-office-worker.ecommoda-dev.workers.dev
الواجهة     : https://ecommoda-dev.github.io/Warehouse-Operations-Center/office-transfer.html
tool في D1  : metafields_change        ← 🔴 سجل **مشترك**
extra.sourceTool : package_transfer_to_office   ← ده اللي بيفصل الأداة عن غيرها
مجموعة السر : warehouse_ops          ← 🔴 مش سر فريد للأداة دي
```

## الحقول اللي بتتكتب

| الحقل | إمتى |
|---|---|
| `custom.package_whereabouts_s1` | الشحنة الأصلية (`S1 = Ready`) |
| `custom.package_whereabouts_s2` | دورة الاستبدال/الاسترجاع (`S1 = Delivered` و`S2 = Ready`) |

القيمة اللي بتتكتب **دايمًا `Office`**. القيم المسموحة في التعريف الحيّ:
`Warehouse · Office · Courier`.

> 🔴 **النوع لازم يبقى `single_line_text_field` بالحرف** — `metafieldsSet`
> بترفض **الكتابة كلها** لو النوع مش مطابق، والرسالة بتبقى غامضة. عشان كده
> `?action=diag` بيقرا **التعريف الحيّ من شوبيفاي** ويقارن النوع وقايمة
> الاختيار: تغيير التعريف من الأدمن بيبان في ٥ ثواني بدل ما يطلع كفشل كتابة
> على أول سكانة من الموظف.

## شروط الأهلية — ومكانها **اتنين بقرار**

| الشرط | ليه |
|---|---|
| ① **مش ملغي** | الأوردر الملغي بيترفض برسالة «قطع الشحنة ورجّع المنتجات على الرف». ده **بالظبط** السيناريو اللي الحقل اتعمل عشانه: أوردر اتغلّف وبعدين اتلغى، والموظف خده المكتب مع الدفعة وفضل هناك |
| ② **الزون قاهرة+جيزة أو شو روم، والمندوب مش `Bosta`** | 🔴 بوسطة **خارج النطاق بالكامل** — بتستلم من المخزن نفسه وطردها عمره ما بيعدّي على المكتب (`ecommoda-order-lifecycle` قاعدة ١٧) |
| ③ **الحالة `Ready`** | `S1 = Ready` → `_s1` · `S1 = Delivered` و`S2 = Ready` → `_s2` (نفس شرط سكانر بوسطة بالحرف) |
| ④ **اتغلّف فعلاً** | وقت تغليف مسجّل على **ميتافيلد الماكينة بتاعتها** |
| ⑤ **العهدة `Warehouse` أو فاضية** | `Office` → «خلاص متعمل» · `Courier` → رفض |

> 🔴 **القاعدة واحدة، ومكان تنفيذها اتنين — وده مقصود:**
> **الواجهة** (`wocOfficeGate` في `shared/shell.js` §OFFICE-GATE) بتقرر **إيه
> اللي يتعرض في الطابور**، و**الـ Worker** (`§SCAN::evaluate`) بيقرر **إيه
> اللي يتكتب** — وهو **المرجع**.
> ⚠️ حارس في الواجهة بس **مش حارس** (`ecommoda-order-lifecycle` §1.5): تاب
> مفتوح من ساعة والأوردر اتلغى في الوقت ده كان هيعدّي، لأن الواجهة حكمت على
> لقطة قديمة. ودي نفس الحفرة اللي أنتجت R3 و§ELIGIBILITY في أداة التغليف.
> ⛔ وأي تعديل على القاعدة **لازم يحصل في المكانين في نفس التمريرة** (درس R1).

> 🔴 **④ أخطر شرط في القايمة — وفشله صامت.** صف S2 بيتفحص بـ
> `custom.s2_packing_date_time` **مش** `s1_…`. الفحص بـ`s1_…` على صف S2 بيعدّي
> على طرد **ما اتغلّفش خالص**:
> **قياس حيّ 15-09-2026:** ٦ أوردرات `S2 = Ready` على المتجر — **الستة كلهم**
> عندهم `s1_packing_date_time`، و**٢ بس** عندهم `s2_packing_date_time`.
> يعني الفحص الغلط كان هيعدّي **٦ من ٦** والصح **٢**.

## 🔴 أرضية تاريخ الأوردر — رجعت نافذة متحرّكة `٢٥٠` يوم (v1.2.0)

🔴 **تراجع صريح عن الأرضية الثابتة (v1.1.0) — قرار أحمد 19-09-2026، §٩ في
`docs/query-cost-experiment.md` بريبو الهب.** نفس القرار بالحرف والقيمة في
`Package-Transfer-To-Warehouse` — درس R1: أرضيتان مختلفتان على صفّين جنب
بعض في الشاشة الرئيسية بتتقرا عطل.

```js
const ROLLING_WINDOW_DAYS = 250;   // قرار أحمد 19-09-2026 (§٩) — نفس القيمة في أداة المخزن
function computeOrdersSince() {
  const ms = Date.now() - ROLLING_WINDOW_DAYS * 24 * 60 * 60 * 1000;
  return new Date(ms).toISOString().slice(0, 10);   // YYYY-MM-DD — UTC
}
```

- ⚠️ **السبب هنا أضعف منه في أداة المرتجعات — ولازم يتقال صريح.** مصدر
  الطابور هنا `manual_status:'Ready'`، وهي حالة **عابرة** (عشرات مش آلاف)،
  فمشكلة تكلفة الاستعلام المتراكمة اللي دفعت القرار في أداة المرتجعات **مش
  منطبقة هنا بنفس القوة**. القيمة اترجعت **عشان الاتساق بين الأداتين** (نفس
  الحقل، نفس الشاشة الرئيسية، درس R1) مش عشان أداة المكتب عندها مشكلة تكلفة
  مستقلة.
- 🔴 **القرار اتاخد وهو عارف الثمن.** الأرضية الثابتة اتحطّت أصلاً (v1.1.0)
  عشان تمنع نافذة متحركة تخفي طرد قديم منسي بالظبط لما يبقى منسي فعلاً.
  الثمن ده **لسه صحيح**، بس ٢٥٠ يوم (~٨ شهور) مساحة أمان أكبر بمراحل من أي
  نافذة قصيرة سابقة.
- ⚠️ **شبكة الأمان المطلوبة — بند مفتوح، راجع «مسائل مفتوحة».**
- 🔴 **`computeOrdersSince()` بتتحسب لحظيًا في كل نداء** — مش ثابت وقت
  الـ cold start.
- ⚠️ **والفلتر على `created_at` مش `updated_at`** — تاريخ **الأوردر** هو اللي
  الموظف بيقراه في الجدول وبيتصرّف عليه؛ و`updated_at` بيتحرّك مع أي تعديل
  (حتى كتابة الميتافيلد بتاعتنا نفسها)، فكان بيدّي أرضية **بتتزحلق تحت الطرد
  نفسه**.
- ⚠️ **والأرضية على الاستعلامين** — واحد من غيرها معناه إن ماكينة بتعرض أقدم
  من التانية، والجدول بيخلط النطاقين **بلا أي إشارة**.
- 🔴 **وهي أرضية عرض — مش شرط أهلية.** الأوردر الأقدم منها **بيتسكن عادي**
  والـ Worker بيكتب عليه؛ هو مش في القايمة وبس. ⛔ ممنوع تتحط في `evaluate`.
- ⚠️ **والصيغة `YYYY-MM-DD` بتتقرا UTC عند شوبيفاي** — الحساب بـUTC مباشرة
  بلا تحويل توقيت القاهرة، لأن فرق يوم على نافذة ٢٥٠ يوم أثره العملي صفر
  تقريبًا.
- 🔴 **والأرضية بترجع في الرد (`ordersSince`) والواجهة بتعرضها من هناك** —
  مش مكتوبة بالإيد في الصفحة. تاريخان يفترقوا في صمت هو درس R1 بالحرف، وفيه
  بند في `docs/office-check.mjs` بيقفله.
- 🔴 **و`office.min` في الـ shell فضلت `1.1.0`** — الرد لسه فيه `ordersSince`
  بنفس الاسم والشكل، فالتدهور آمن على Worker أقدم: على `1.0.0` بس (قبل
  `ordersSince` خالص) الطابور بيرجّع **كل** أوردرات `Ready` مهما كان قِدَمها،
  والسطر اللي تحت عنوان الطابور **بيختفي** (الواجهة بتعرضه من `ordersSince`
  وبس) —
  يعني الرقم بيكبر **في صمت** والسطر الوحيد اللي كان هيقول السبب مش موجود.

## 🔴 ليه الفلترة في الواجهة والـ Worker بيرجّع خام؟

`custom.package_whereabouts_s1` **مش قابل للفلترة** في بحث شوبيفاي. الفلتر
عليه **بيتجاهَل في صمت** ويرجّع **المتجر كله**:

```
metafields.custom.package_whereabouts_s1:'Warehouse'  →  10,000 صف (مقيس 15-09-2026)
```

فالـ Worker بيستعلم عن **الحالة بس** (`manual_status:'Ready'` و
`status_2_r_e:'Ready'`) وبيرجّع الحقول الخام، والأهلية بتتحسب في الـ shell.

> ⛔ **ممنوع تحاول تفلتر على الحقل ده في `query`** — الرد هيعدّي بـ`200`
> وبمصفوفة فيها المتجر كله، والطابور هيقول رقم مالوش أي علاقة بالواقع.

## الـ Endpoints

| `action` | Method | بيعمل إيه |
|---|---|---|
| `get_config` | GET | `WORKER_VERSION` — الواجهة بتقارنه بـ`min` |
| `diag` | GET | فحص ذاتي بلا كتابة — **مصفوفة `[{ ok, label, detail, hint }]`** |
| `get_employees` | GET | فلتر الموظف في تاب السجل |
| `get_ready_to_office` | GET | أوردرات `Ready` (الماكينتين) من `2026-04-01` فأحدث بحقولها الخام |
| **`scan`** | **POST** | قراءة الأوردر حيًّا + الحكم + `metafieldsSet` + صف D1 |
| `get_logs` · `get_logs_count` · `get_logs_export` | GET | تاب السجل |

> 🔴 **مفيش `check_employee`/`register_pin`/`verify_employee`/`log_logout`
> — انحراف مقصود.** الأداة مالهاش واجهة مستقلة، والدخول بيحصل **مرة واحدة**
> في `index.html` بتاع الهب عبر Worker التغليف. نقطة دخول تانية هنا = سطح
> هجوم بلا مستهلك. نفس قرار `order-sku-barcode-printer-worker`.
> ⚠️ و`get_employees` **موجود** لأنه مستهلَك فعلاً — من غيره فلتر الموظف في
> السجل بيفضل فاضي **بلا أي خطأ**.

### عقد `scan`

```jsonc
// الطلب
{ "code": "7212244533570", "ack": false, "employee": "ahmed" }

// الرد — خمس نتايج
{ "result": "success"   }  // اتكتبت واتأكدت من رد شوبيفاي
{ "result": "warning"   }  // اتكتبت، وكان فيه حاجة الموظف أقرّ بيها
{ "result": "error"     }  // النداء وصل لشوبيفاي واترفض — ما اتكتبتش
{ "result": "rejected"  }  // اتوقف قبل أي محاولة — بالسبب
{ "result": "already"   }  // العهدة `Office` أصلاً — **محايدة مش فشل**
{ "result": "needs_ack" }  // حالة شاذة — مفيش كتابة ومفيش صف لحد الإقرار
```

> 🔴 **قاعدة عدم الضياع:** كل سكانة بتسيب **صف واحد بالظبط** في D1 — يا
> `update` يا `rejected`. صفر صفوف = سؤال «الأوردر ده اتسكن ولا لأ؟» مالوش
> إجابة بعد ما الشاشة تتقفل.
> ⚠️ **الاستثناء الوحيد `needs_ack`** — مفيش قرار اتاخد لسه، والسكانة
> **هتكمّل** بصف كامل بعد الإقرار. صف هنا كان هيبقى صفّين لسكانة واحدة.

> 🔴 **`already` ممنوع تتعدّ فشل وممنوع تاخد لون تحذير** (`ecommoda-constants`
> §12). في الأدوات الشقيقة دي كانت **٥٩ من ٦١** صف فشل و**٦ من ٧** صف رفض —
> يعني أكتر نتيجة بتتكرر، ولو اتلوّنت أحمر الموظف بيتعلّم يعدّي على الأحمر كله.

### فخّ البحث بالاسم

الرقم **١٠ خانات فأكتر** = ID رقمي → **قراءة بمفتاح** (`order(id:)`).
أقل من كده = اسم أوردر → **بحث** (`orders(query:"name:#54727")`).

> 🔴 **البحث بالاسم بحث مش قراءة** — `name:#5362` بيطابق `#53621` كمان.
> المطابقة التامة على الاسم الراجع **إلزامية**، ومن غيرها الأداة بتكتب
> `Office` على **أوردر تاني** (`shopify-graphql-helper` §name-search).

## D1

```
tool             : metafields_change              ← سجل مشترك بين أكتر من أداة
extra.sourceTool : package_transfer_to_office     ← الفاصل الوحيد بين الكُتّاب
type             : update · rejected
```

| `type` | `extra.result` | معناه |
|---|---|---|
| `update` | `success` · `warning` · `error` | اتبعت لشوبيفاي — نجح أو اترفض |
| `rejected` | `rejected` · `already` | اتوقف **قبل** أي محاولة |

> ✅ **صفر قيمة `type` جديدة** — `update` و`rejected` **مسجّلتين أصلاً** لصف
> `metafields_change` في `ecommoda-constants` §7، فـRule 7 مالهاش نطاق جديد
> هنا. اللي **لسه مطلوب** هو تسجيل الأداة في **جدول الكُتّاب** (§7 بيقول
> صراحةً: أي أداة جديدة بتكتب تحت `metafields_change` **تسجّل نفسها**).

مفاتيح `extra`: `result` · `stage` (`lookup`/`write`) · `machine` (`S1`/`S2`) ·
`zone` · `courier` · `s1` · `s2` · `packedAt` · `packedBy` · `code` ·
`acknowledged` · `warnings[]`.

و`value_before`/`value_after` = **عهدة الطرد** قبل وبعد (`—` → `Office`).

> 🔴 **الدخول/الخروج مش هنا** — بيتسجّلوا تحت `warehouse_ops_center` من
> Worker التغليف (الهب هو اللي بيسجّل دخوله). الأداة دي **مابتكتبش**
> `login`/`logout` خالص.

> 🔴 **استعلام خط الأساس لازم يفلتر على `extra.result`** — الاستعلام اللي
> بيعدّ `type` بس **بيقيس المحاولات مش الكتابة الفعلية**:
> ```sql
> SELECT COUNT(*) FROM logs WHERE tool = 'metafields_change' AND json_extract(extra,'$.sourceTool') = 'package_transfer_to_office' AND type = 'update' AND json_extract(extra,'$.result') = 'success';
> ```
> ⚠️ **و`extra.sourceTool` جزء من الاستعلام مش تزويق** — من غيره الرقم بيشمل
> صفوف سكانرَي بوسطة.

> 🔴 **ليه السجل المشترك؟ (قرار أحمد 15-09-2026)** الأداة بتكتب ميتافيلد،
> و`metafields_change` هو **سجل تغيير الميتافيلد المشترك** في الستاك — نفس
> مكان سكانرَي بوسطة بالحرف، وبنفس نمط الفصل (`extra.sourceTool`).
> ⚠️ **والثمن لازم يتقال:** الصف ده كمان هو المصدر اللي
> `ecommoda-order-lifecycle` قاعدة ٩ بتسمّيه لتاريخ **حالة الأوردر**. والأداة
> دي **مابتلمسش `manual_status` ولا `status_2_r_e` خالص** — بتكتب **عهدة
> الطرد**، محور تاني تمامًا. يعني أي استعلام ساذج بيعدّ
> `tool='metafields_change' AND type='update'` بقى **بيضخّم انتقالات الحالة**
> بصفوف عهدة.
> ⛔ **اللي بيمنع ده هو `extra.sourceTool` — وهو إلزامي في كل استعلام**، مش
> اختياري. والـ Worker بيطبّقه في التلات endpoints بتاعة السجل وفي `diag`.

> 🔴 **انحراف موثّق عن `ecommoda-constants` §7 — مفتاح `sourceTool` مش
> `source`.** §7 بتقول: «أي أداة جديدة تكتب تحت `metafields_change` تستخدم
> **`extra.source`** (مش `sourceTool`)». **قرار أحمد 15-09-2026: يبقى
> `sourceTool`** — عشان يبقى **مطابق حرفيًا** لسكانرَي بوسطة، فأي استعلام
> بيفصل الكُتّاب بيبقى بمفتاح واحد بدل اتنين.
> ⚠️ **والانحراف ده لازم يتسجّل مع تسجيل الأداة في جدول الكُتّاب** — وإلا أول
> جرد بيتكتب على `extra.source` بيرجّع **صفر صف** للأداة دي، بلا أي خطأ.

## CORS

**Option B — قايمة صارمة.** الأداة **بتكتب على شوبيفاي**، فمفيش wildcard:

```js
const ALLOWED_ORIGINS = ['https://ecommoda-dev.github.io'];
```

## النشر — الخطوات اليدوية (كلها حاجزة)

| # | الخطوة | من غيرها |
|---|---|---|
| ① | إنشاء الـ Worker باسم **`package-transfer-to-office-worker`** بالظبط | الرابط في `WOC_WORKERS.office.url` مايردّش |
| ② | ربط Workers Builds على الريبو ده (`main`) | أي push مابينشرش |
| ③ | **`WORKER_SECRET` = قيمة مجموعة `warehouse_ops`** → **Promote** | كل نداء `401` |
| ④ | `CLIENT_ID` + `CLIENT_SECRET` → **Promote** | OAuth بيفشل والطابور بيقع |
| ⑤ | تسجيل الأداة في **جدول كُتّاب `metafields_change`** في `ecommoda-constants` (§7 · `references/d1-history.md`) — ومعاه انحراف `sourceTool` | جرد بيعدّ الصف ده كأداة واحدة **بيغلط**، وجرد بيدوّر على `extra.source` **بيرجّع صفر** |

> 🔴 **③ أهم واحدة فيهم، والفخّ إن `WORKER_SECRET` هنا مش قيمة فريدة.** الهب
> بيبعت **سر واحد** لكل الـ Workers من مفتاح `localStorage` واحد
> (`warehouse_ops_worker_secret` · Standards #39). سر خاص بالأداة = `401` على
> كل نداء، والصفحة بتفتح والهيدر شغّال وأول استعلام بيقع.
> ⚠️ **والأداة دي أسهل من الأدوات اللي قبلها في النقطة دي** — مالهاش نسخة
> مستقلة، فمفيش سر قديم لازم يتلزق في مكانين.
> ⚠️ `?action=diag` بيثبّت الحاجة دي: بند «متغيرات وأسرار الـ Worker».

> ⚠️ **`[vars]` في `wrangler.toml` بس** — ممنوع تضيف `SHOP_DOMAIN` بإيدك في
> الداشبورد لأداة منشورة من git: أول push بيدهس القيمة اليدوية (واقعة موثّقة
> على `stylebox-price-sync-worker` · 12-09-2026).

## فحص المتصفح

الفحص بتاع الأداة دي في ريبو الهب (الواجهة عايشة هناك):

```bash
cd Warehouse-Operations-Center
npm i playwright postcss --no-save
node docs/office-check.mjs      # ٦٤ بند
```

> 🔴 **تلات عيلات فشل صامتة الملف ده اتكتب عشانها**, ومستحيل يتمسكوا
> بمراجعة كود:
> ① شرط أهلية اتكتب في الصفحة بدل الـ shell → الطابور والرئيسية يقولوا رقمين
>   مختلفين **وصفر خطأ في الكونسول** (درس R1 · v1.11.0).
> ② صف S2 بيتفحص بوقت تغليف S1 → طرد ما اتغلّفش بيعدّي **بلا أي رسالة**.
> ③ الصف مابيتشالش من الطابور بعد النجاح → الشاشة زي ما هي فالموظف بيسكن
>   تاني (٥٩ صف فشل كذّاب في سكانر المرتجعات).

> ⚠️ **والبند ده مسك عطل فعلي وقت البناء:** `loadCounts()` في `index.html`
> كانت بتنادي `loadQueue` بقايمة **مكتوبة بالإيد** (`['print','pack','bosta']`)
> — فصف المكتب اترسم وعدّاده فضل `—` للأبد، **بلا أي خطأ**. بقت بتتبني من
> `Object.keys(homeQ)`.

## 🏷️ عمود «موقع الشحنة» في الواجهة (هب v1.30.0 — صفر تعديل Worker)

الطابور بقى فيه عمود بيقول **العهدة المسجّلة على الطرد قبل السكانة**.
**صفر تعديل هنا:** `w1`/`w2` بيرجعوا في `ORDER_FIELDS` **من `1.0.0`** —
البوابة في الـ shell بتقرا منهم أصلاً، واللي اتضاف سطر واحد في
`wocOfficeQueue` بيلزق القيمة على الصف.

- 🔴 **والقيم اللي ممكن تظهر في العمود ده اتنين بس** — `Warehouse` أو **فاضي**.
  `wocOfficeGate` بتستبعد `Office` («في المكتب خلاص») و`Courier` («مع
  المندوب») وأي قيمة بره القايمة **قبل** ما الصف يدخل الطابور.
- ⚠️ **وهيقول «— مش مسجّل» على كل الصفوف النهاردة** — أداة التغليف لسه ما
  بتكتبش `Warehouse` وقت التغليف (بند مفتوح تحت)، فالحقل فاضي على كل أوردر في
  المتجر. **الثمن ده اتقال قبل التنفيذ واتوافق عليه**، والعمود بيبقى مفيد أول
  ما التغليف يبدأ يكتب.
- ⚠️ **والفاضي «مش مسجّل» مش «في المخزن»** — «مش معروف» ≠ «هنا».

## مسائل مفتوحة

- 🔴 **الأربع خطوات النشر اليدوية فوق — كلها حاجزة** (إنشاء الـ Worker ·
  Builds · السر · OAuth).
- 🔴 **تسجيل الأداة ككاتب جديد في `metafields_change`** (`ecommoda-constants`
  §7 · `references/d1-history.md`) — **مش صف `tool` جديد**: `update` و
  `rejected` مسجّلتين أصلاً، فصفر قيمة `type` جديدة وRule 7 مالهاش نطاق جديد.
  اللي مطلوب: الأداة تتسجّل في **جدول الكُتّاب**، ومعاها **انحراف
  `extra.sourceTool` بدل `extra.source`** (قرار أحمد).
  ⚠️ من غير ده أي جرد بيعدّ `metafields_change` كأداة واحدة **بيغلط**، وأي
  جرد بيدوّر على `extra.source` **بيرجّع صفر صف** للأداة دي — **بلا أي خطأ**.
- 🔴 **الأداة التانية (شبكة الأمان) لسه ما اتعملتش** — «الطرد اللي عهدته
  `Office` والأوردر ملغي/مرتجع → سكان يرجّعه `Warehouse`». من غيرها **الطرد
  المنسي في المكتب لسه بند مفتوح في الواقع** حتى والحقل بيتكتب:
  قيمة في شوبيفاي ومحدش بيقراها بالعكس مابتمسكش حاجة
  (`ecommoda-order-lifecycle` → `known-gaps.md` **G-17**).
- 🟡 **أداة التغليف لسه ما بتكتبش `Warehouse`** — هي «الأداة رقم ١» في خطة
  المهارة. لحد ما تتعدّل، الحقل بيفضل **فاضي على كل أوردر**، وعشان كده الشرط
  ⑤ بيقبل **الفاضي**. ⛔ ممنوع الفاضي يترفض قبل ما أداة التغليف تكتب — الطابور
  هيفتح **فاضي**.
- 🟡 **البداية الباردة** (قرار أحمد: نسيبها) — أول تشغيل الطابور هيفتح
  بأوردرات متغلّفة من أسابيع، وبعضها راح المكتب فعلاً من غير ما يتسجّل. متوقّع،
  والقايمة بتنضّف نفسها مع أول دورة.
- 🟡 **إعادة الطباعة بعد التغليف مش شرط استبعاد** — طابور بوسطة بيستبعد
  الأوردر اللي `printed > packed` (يعني الفاتورة اللي جوّه الطرد لاغية والطرد
  محتاج يتفتح). الشرط ده **مش مطلوب في مواصفات أحمد** فما اتنفّذش. لو اتقرر
  يتضاف، مكانه `evaluate` في الـ Worker و`wocOfficeGate` في الـ shell **في
  نفس التمريرة**.
- 🟢 **الحقل مش بيبوّب أي فعل** — `package_whereabouts` مابيمنعش طباعة ولا شحن
  ولا إلغاء. السؤال ده مفتوح في المهارة (§7) ومحدش طلبه.
- 🔴 **شبكة الأمان المطلوبة مع النافذة المتحرّكة — بند مفتوح لازم يتنفّذ
  (v1.2.0 · قرار أحمد 19-09-2026 §٩).** راجع القسم فوق. المطلوب حاليًا:
  **تقرير دوري يدوي** (مرة كل شهر) بيراجع أوردرات `Ready` الأقدم من
  `computeOrdersSince()` الحالية. الحل الدائم (فهرس D1) **مؤجّل**.

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | **v3.3.0** |
| ecommoda-constants | **v2.6.0** |
| ecommoda-order-lifecycle | **v1.8.0** |
| shopify-graphql-helper | **v1.1.0** |
| ecommoda-html-builder | **v6.6.0** |

آخر مطابقة: 19-09-2026 · الـ Worker `1.2.0`
🔴 معلّقة: **إنشاء الـ Worker + ربط Builds + `WORKER_SECRET` (سر `warehouse_ops`)
+ `CLIENT_ID`/`CLIENT_SECRET` → Promote** · **تسجيل الأداة ككاتب جديد على
`metafields_change` في `ecommoda-constants` §7 (بقيمة `extra.sourceTool`)** ·
تحديث `ecommoda-order-lifecycle`
(`package-whereabouts.md` §5: الأداة بقت **أداتين** مش واحدة) ·
**شبكة الأمان (تقرير دوري) للنافذة المتحرّكة الجديدة**

---

آخر تحديث: 19-09-2026 — `1.2.0` (🔴 **§٨+§٩ من `docs/query-cost-experiment.md`
بريبو الهب — قرار أحمد النهائي.** §٨②: `getAccessToken()` بقت بنفس انضباط
`shopifyGQL` (retry + backoff) — تصحيح عام للخمس Workers المشتركة في نفس الـ
Custom App. §٨④: أضيف نمط `queryCost` + بند `actualQueryCost` في `diag` (كان
غايب هنا، موجود في أداة المرتجعات من v1.3.0). §٩: الأرضية الثابتة
(`2026-04-01`) اترجع فيها لنافذة متحرّكة `٢٥٠` يوم (`ROLLING_WINDOW_DAYS`) —
نفس القيمة بالحرف في `Package-Transfer-To-Warehouse` (درس R1). ⚠️ **تراجع
مُعلَن عن قرار v1.1.0** — راجع قسم «أرضية تاريخ الأوردر» فوق. صفر تغيير في
عقد الرد، `office.min` في الـ shell فضلت `1.1.0`)

19-09-2026 — `1.1.0` (أرضية تاريخ أوردر ثابتة
`created_at >= 2026-04-01` على الاستعلامين · `ordersSince` في الرد ·
بند جديد في `diag` — قرار أحمد 19-09-2026)

15-09-2026 — `1.0.0` (أول إصدار)

</div>
